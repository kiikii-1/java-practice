# 「会員管理システム」設計書

## 要件
- 会員を登録できる
- 会員一覧を表示できる
- IDで検索できる

---
## 用語 🎯
-
---

## クラス
**main**
- main()
managerのオブジェクトを作成
manageオブジェクトそれぞれにaddUser()で会員情報入力

nextInt() //探したいidの読み取り
findUserById() //探したいidをもとにUserを返す

- if　//UserあればNameを返す

**user**
User型で
- id
- name
を持ち、コンストラクタでmainのフィールドと対応させる

メソッド
- getId()　//Userの中身を見る
- getName()

**UserManager**
フィールド
- Map<Integer, User> users

メソッド
- addUser() //User型のuserをmapであるusersに入れる
- findUserById() //MapからUserを探してくる

---

## クラス関係
main- user - search
